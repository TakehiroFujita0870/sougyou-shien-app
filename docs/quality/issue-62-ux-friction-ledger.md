# Issue #62 UX friction ledger

Scope: current main PII-free local journey, reviewed at desktop and 390px.

| Finding | Evidence | Actionable next step |
| --- | --- | --- |
| Keyboard surface shortcuts are not discoverable in the UI. | `Alt+Shift+1..3` works but is not explained next to navigation. | Design may add a compact shortcut hint when the shell next changes. |
| Candidate decision state is local-only and has no visible restore cue. | A refresh restores it, but the user receives no confirmation. | Add a small local-save status only with the next candidate UX slice. |

No product defect was found in the assigned flow. This ledger does not authorize UI changes.
