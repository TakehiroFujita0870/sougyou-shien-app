# Issue #69 アイデア入口統合 計画

最終検証日: 2026-08-09

## 要望 / ゴール / 成功指標

要望: アイデア入力の二重導線を解消し、Notion型の縦UI内で対話、仮説カードのプレビュー、明示保存を一つの主導線にする。巨大heroは表示しない。

ゴール: 利用者が着想を会話で整理し、保存前に生成元を確認した上で端末内の候補として保存できる。

成功指標: idea landingに旧3項目フォームと巨大heroを表示せず、PCとmobileのStoryおよび自動テストで対話、プレビュー、保存、再開を確認できる。

## ユーザーストーリーと受け入れ条件

### US-1

As a 起業を考え始めた利用者, I want 着想を一つの対話欄から話す, so that 同じ内容を複数フォームに入力しない。

Given: 事業のタネのlandingを開いている
When: 入力欄に着想を送り対話を進める
Then: 保存前プレビューが表示され、明示した保存操作でのみ候補になる。

### US-2

As a 再開する利用者, I want F5や低速読込後も未送信入力を守る, so that 考え途中の内容を失わない。

Given: 端末内の会話または旧3項目下書きがあり、読込が遅延している
When: 読込完了前に新しい入力を始める
Then: 新しい未送信入力は読込結果で上書きされず、旧下書きは会話へ移行できる。

### US-3

As a プライバシーを重視する利用者, I want ローカルだけで補完案を確認する, so that 外部送信なしで候補を作れる。

Given: 対話欄に着想を入力している
When: 補完案を要求する
Then: 決定的なローカル補完案だけをプレビューし、採用するまで原文を変更しない。

### US-4

As a 候補を検討する利用者, I want 採用、理由付き却下、保留を自分で選ぶ, so that AI候補に従わず判断を引き継げる。

Given: メインAIが会話から候補を提示している
When: 利用者が採用、理由付き却下、または保留を選ぶ
Then: 採用だけが事業のタネへ反映され、保留は候補として残り、却下理由がある同じ前提の候補は再提示しない。前提が変わった場合だけ再提示する。

## UI情報構造

- #69: 「事業のタネ」ページはbreadcrumbの直下に簡潔なページ見出しを置き、会話、保存前プレビュー、保存済み候補を縦に読む。
- #69: 会話中の利用者発言だけから端末内で芽を検出し、保存前のカードを表示する。候補の保存と編集は明示操作に限る。
- 次PR: 候補カードに採用、理由付き却下、保留を表示し、既存の候補データに判断状態と前提を保存する。却下済み候補は同じ前提で表示せず、会話から前提差分を検出した場合だけ再提示する。
- 依存: 全ページ共通AI会話composer、desktop常設/即時展開、mobile固定入口、現在page/project contextは#74/#78に接続する別PRで扱う。#69はWorkspaceShellの構造・環境badge・共通libraryを変更しない。

## 質問リスト

| ID | 質問 | 決定者 | 期限 |
| --- | --- | --- | --- |
| Q-1 | 実サービスの永続化先 | CEO | バックエンド実装PR前 |
| Q-2 | 前提変化を判定する最小の比較単位 | CEO | 判断状態を実装するPR前 |

## スコープ外

- 外部AI送信、APIキー、課金開始
- 固定Stageまたは通過gate
- 候補のサーバー永続化と既存候補の削除
- #74 WorkspaceShellの構造変更
- 全ページ共通composer、page/project context、space共通library
- 理由付き却下、保留、前提差分による再提示抑制（次PRで既存候補データへ実装）

## タスク

| ID | 成果物 | 完了判定（検査:） | 不確実性 |
| --- | --- | --- | --- |
| T-1 | landingの対話導線統合と巨大hero撤去 | 検査: AppテストでIdeaFormと巨大hero非表示 | 既知 |
| T-2 | 下書きhydrate競合保護と旧下書き移行 | 検査: IdeaCandidateWorkspaceテスト | 類推可能 |
| T-3 | PC/mobile Storyとa11y確認 | 検査: npm run test:a11y と Storybook build | 既知 |
| S-1 | 前提変化の比較単位スパイク | 検査: Q-2の決定と既存候補データでの表現可否を記録する | 未知 |
| T-4 | 候補の採用・理由付き却下・保留と再提示抑制 | 検査: 判断状態と前提差分のテスト | 類推可能 |

## ADR

| 判断 | 選択と理由 | 却下案と理由 | 結果 |
| --- | --- | --- | --- |
| 補完方法 | 決定的なローカル関数を使い、端末外送信を発生させない | 外部LLM呼出は同意、鍵、運用境界が未整備のため却下 | 採用 |
| 旧下書きの扱い | 会話が空の場合だけ会話の最初の発言へ移す | 旧フォームを並行表示すると二重導線が残るため却下 | 採用 |
| 共通composer | #69では変更せず、#74/#78に接続する別PRで全ページへ置く | #69へshell変更を混在させると500行と単一目的の規律を超えるため却下 | 採用 |
| 保守性 | 既存`IdeaCandidateWorkspace`、React state/effect、Tailwind utilityを再利用する | styled UI基盤、独自dialog/menu、依存追加は既存機能で足り、保守責任を増やすため却下 | 採用 |
| legacy変換 | 既存3項目の端末内値を一回の会話発言へ変える小関数だけを置く。旧キーの利用者がいなくなり、永続化先が確定したPRで削除する | 第二のrepositoryや移行専用保存先は重複永続化になるため却下 | 採用 |
| 候補判断 | 採用、理由付き却下、保留は利用者が選び、採用だけを事業のタネへ反映する。却下済みは同じ前提で再提示しない | AIの自動保存や固定gateは利用者判断を奪うため却下 | 次PR |
| 再提示抑制 | 判断状態を既存候補データへ保存し、会話の前提差分が確認できた場合だけ再提示する | 独立したdecision repositoryは重複永続化になるため却下 | Q-2決定後 |

## 変更履歴

| 日時 | 変更 | 理由 | 影響タスク |
| --- | --- | --- | --- |
| 2026-08-09 | 初版 | Issue #69の実装開始 | T-1, T-2, T-3 |
| 2026-08-09 | 共通composerを依存PRへ分離し、巨大hero撤去を明記 | #69をconversation-to-seedに固定する運用判断 | T-1, T-3 |
| 2026-08-09 | 再利用とlegacy変換の保守境界を追記 | UI基盤・永続化の独自実装を避ける保守性ゲート | T-1, T-2 |
| 2026-08-09 | 候補判断と再提示抑制を次PRの計画へ追加 | 確定プロダクト判断を反映し、基盤PRの完了範囲と分離する | T-4 |
