import { describe, it } from 'vitest';

describe('UI UX contract: intentionally failing acceptance scenarios', () => {
  it.todo('FAIL-UX-01: every page exposes one AI wall-chat entry for mouse, keyboard, and screen reader');
  it.todo('FAIL-UX-02: conversation produces a business-seed preview and saves only after explicit approval');
  it.todo('FAIL-UX-03: approved attachments are reusable from the space library in another page and project');
  it.todo('FAIL-UX-04: slow hydration keeps saved profile, conversation, preview, and library state through F5');
  it.todo('FAIL-UX-05: the full path is operable at 1280px and 390px with keyboard and screen reader semantics');
  it.todo('FAIL-UX-06: giant hero, separate IdeaForm, and local-fake warning do not block the primary path');
});
